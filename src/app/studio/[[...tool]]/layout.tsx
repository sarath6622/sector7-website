/**
 * Studio layout — intentionally minimal.
 * No Navbar, Footer, or WhatsApp FAB. The Sanity Studio provides its own UI.
 */
export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
